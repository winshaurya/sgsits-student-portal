###############################################
# REQUIRED ENVIRONMENT VARIABLES (NO OPTIONS)
# Copy to .env and adjust secrets for your deployment.
# Do NOT commit the real .env file.
###############################################

# MySQL connection (single DSN string parsed by Data.php)
DB_HOST=mysql://root:ekttgZMWoIHuHgOEGFMeRyFPcSRnZxyK@shortline.proxy.rlwy.net:40234/railway

# These must match the DSN above (kept explicit for clarity & legacy code paths)
DB_PORT=40234
DB_NAME=railway
DB_USER=root
DB_PASS=ekttgZMWoIHuHgOEGFMeRyFPcSRnZxyK

# Enable verbose PHP errors (set to 0 in production)
DEBUG=1

# Application metadata
OPEN_SIS_TITLE="GS Student Portal"
OPEN_SIS_NOTIFY=winshaurya9@gmail.com

###############################################
# SECURITY: Replace the password and rotate if exposed.
###############################################
