###############################################
# Environment Example (.env)
# Choose ONE database configuration style below.
# NOTE: If you use a single MySQL URL (DSN) do NOT also override DB_PORT/DB_NAME
# with conflicting values or the connection can fail.
###############################################

########## OPTION A: Single DSN (simplest) ##########
# Preferred: put full URL in DB_URL (or DB_HOST). Leave discrete vars UNSET.
# Uncomment and adjust:
DB_URL=mysql://root:CHANGE_ME_PASSWORD@shortline.proxy.rlwy.net:40234/railway
# When DB_URL (or a mysql:// DB_HOST) is used, DO NOT also set DB_PORT/DB_NAME differently.

########## OPTION B: Discrete variables ##########
# Use these ONLY if not supplying a DSN above. Comment out DB_URL if you use these.
# DB_HOST=shortline.proxy.rlwy.net
# DB_PORT=40234
# DB_NAME=railway
# DB_USER=root
# DB_PASS=CHANGE_ME_PASSWORD

########## DEBUG ##########
# Set to 1 to show PHP errors (disable in production)
DEBUG=0

########## App Metadata ##########
OPEN_SIS_TITLE="GS Student Portal"
OPEN_SIS_NOTIFY=you@example.com

###############################################
# Security Tips:
# 1. NEVER commit the real .env file.
# 2. Use different passwords for dev/staging/prod.
# 3. Rotate credentials if they were ever exposed.
###############################################
